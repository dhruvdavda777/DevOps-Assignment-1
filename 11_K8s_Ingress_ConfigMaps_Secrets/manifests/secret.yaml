# stringData lets me write plain text; the API server base64-encodes it on write.
# NOTE: base64 is encoding, not encryption. This file must never be committed
# with real credentials - see the README.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
stringData:
  DB_USERNAME: "dhruv_admin"
  DB_PASSWORD: "S3cur3-Assignment-Pass"
  API_TOKEN: "tok_24bcs10203_demo_only"
